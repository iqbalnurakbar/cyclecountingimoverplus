# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: cyclecountingimoverplus
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: http://10.6.168.44:8000
          client: '200'
        app:
          name: ZWM318OVERPLUS
          description: Overplus Form
          package: Z_S4HANA_FIORI
          transport: FDCK900203
        exclude:
          - /test/
